/// keep <enum> for each item as its essential to the plugin
/// any classes not included will be unplayable
/// default wep MUST use an id!
"root" {
    "default" "heavy"
    "heavy" {
        "secondary" {
            "default" "42"
            "<enum>" "42"
            "<enum>" "159"
            "<enum>" "433"
            "<enum>" "863"
            "<enum>" "1002"
            "<enum>" "1190"
        }
        "melee" {
            "default" "656"
            "<enum>" "tf_weapon_fists"
        }
    }
}